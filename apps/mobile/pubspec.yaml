name: hybrid_index
description: HYBRID INDEX — app mobile (iOS + Android, base unique ; Web pour démo navigateur).
publish_to: "none"
version: 0.0.1+1

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  flutter_riverpod: ^2.5.1
  fl_chart: ^0.69.0
  shared_preferences: ^2.3.2
  google_sign_in: ^6.2.1
  google_sign_in_web: ^0.12.4
  google_sign_in_platform_interface: ^2.5.0
  in_app_review: ^2.0.9
  share_plus: ^10.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
