name: Build Android APK

# Fabrique l'APK Android d'Athlete League dans le cloud (aucun outil à installer en local).
# Lancement : onglet "Actions" du dépôt GitHub → "Build Android APK" → "Run workflow".
# Résultat : l'APK est téléchargeable en bas de la page du run (section "Artifacts").
on:
  workflow_dispatch: # bouton manuel
  push:
    branches: [main]
    paths: # ne reconstruit sur push que si le code mobile change
      - "apps/mobile/**"
      - ".github/workflows/build-apk.yml"

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/mobile
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.44.2"
          channel: stable
          cache: true

      - name: Dépendances
        run: flutter pub get

      - name: Décoder la clé de signature (si le secret est présent)
        env:
          KS_B64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
        run: |
          if [ -n "$KS_B64" ]; then
            echo "$KS_B64" | base64 -d > "$RUNNER_TEMP/release.jks"
            echo "ANDROID_KEYSTORE_PATH=$RUNNER_TEMP/release.jks" >> "$GITHUB_ENV"
            echo "✓ Clé de release décodée (APK signé avec la clé stable → Google Sign-In OK)."
          else
            echo "⚠ Pas de secret ANDROID_KEYSTORE_BASE64 → signature debug (Google Sign-In KO)."
          fi

      - name: Build APK (release, signé avec la clé stable si dispo)
        env:
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        # --split-per-abi : un APK PAR architecture CPU au lieu d'un APK universel 3-en-1
        # (~ -45 % à télécharger). Sur un téléphone récent : prendre app-arm64-v8a-release.apk.
        run: |
          set -o pipefail
          flutter build apk --release --split-per-abi             --dart-define=API_BASE_URL=https://api-production-9a50.up.railway.app             --dart-define=PUSH_ENABLED=true             --dart-define=BUILD_NUMBER=${{ github.run_number }} 2>&1 | tee apk-build.log

      # Diagnostic : en cas d'échec, les lignes d'erreur du log deviennent des ANNOTATIONS
      # publiques du run (lisibles sans droits admin sur les logs).
      - name: Diagnostic (annotations si échec)
        if: failure()
        run: |
          grep -iE "error|FAILURE|Exception|what went wrong" apk-build.log | head -30 | while IFS= read -r l; do echo "::error::APK: $l"; done

      - name: Publier les APK (téléchargeables dans les Artifacts du run)
        uses: actions/upload-artifact@v4
        with:
          name: athlete-league-apk
          path: apps/mobile/build/app/outputs/flutter-apk/app-*-release.apk
          if-no-files-found: error

      # Le Play Store n'accepte QUE le format AAB (App Bundle), pas l'APK. Même signature/clé
      # que l'APK ci-dessus. À téléverser tel quel dans la Play Console (test fermé puis prod).
      - name: Build AAB (App Bundle pour le Play Store)
        env:
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: >
          flutter build appbundle --release
          --dart-define=API_BASE_URL=https://api-production-9a50.up.railway.app
          --dart-define=PUSH_ENABLED=true
          --dart-define=BUILD_NUMBER=${{ github.run_number }}

      - name: Publier l'AAB (à téléverser dans la Play Console)
        uses: actions/upload-artifact@v4
        with:
          name: athlete-league-aab
          path: apps/mobile/build/app/outputs/bundle/release/app-release.aab
          if-no-files-found: error
